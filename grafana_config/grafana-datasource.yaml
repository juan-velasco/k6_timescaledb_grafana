apiVersion: 1

datasources:
  - name: timescaledb
    type: postgres
    url: timescaledb:5432
    user: postgres
    secureJsonData:
      password: postgres
    isDefault: true
    jsonData:
      database: metrics
      sslmode: disable
      postgresVersion: 1500
      timescaledb: true    